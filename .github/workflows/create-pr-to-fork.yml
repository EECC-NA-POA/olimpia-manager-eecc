name: Create PR to Fork Repository

on:
  push:
    branches: [release]
  workflow_dispatch:

jobs:
  create-pr-to-fork:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout 'release' branch
        uses: actions/checkout@v4
        with:
          ref: release
          fetch-depth: 0 # fetch-depth: 0 é crucial para ter todo o histórico

      - name: Create and Push Branch to Fork
        # Injeta o segredo como uma variável de ambiente segura para este passo.
        # Todos os comandos 'git' neste bloco poderão usar o token.
        env:
          FORK_REPO_TOKEN: ${{ secrets.FORK_REPO_TOKEN }}
        run: |
          # 1. Configurar o Git
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

          # 2. Adicionar o remote do fork usando a variável de ambiente para autenticação
          echo "Adding fork remote..."
          # ESTA É A LINHA CRUCIAL PARA INCLUIR O TOKEN NA URL DO REPOSITÓRIO REMOTO
          # O 'x-access-token' é um "username" especial para autenticação com PATs.
          git remote add fork "https://x-access-token:${FORK_REPO_TOKEN}@github.com/EECC-NA-POA/olimpia-manager-eecc.git"
          
          git fetch fork --depth=1 # Apenas para verificar a conexão, não precisa de todo o histórico do fork
          echo "Remotes configured:"
          git remote -v

          # 3. Criar uma branch única a partir da 'release'
          BRANCH_NAME="sync-from-release-$(date +%Y%m%d-%H%M%S)"
          echo "Creating branch: $BRANCH_NAME"
          git checkout -b "$BRANCH_NAME" # Usar aspas para garantir que o nome da branch seja interpretado corretamente

          # 4. Fazer o push da nova branch para o fork
          echo "Pushing branch to fork..."
          # Agora, o remote 'fork' já tem a autenticação configurada
          git push -u fork "$BRANCH_NAME"

          # 5. Salvar o nome da branch para o próximo passo
          echo "BRANCH_NAME=$BRANCH_NAME" >> "$GITHUB_ENV" # Usar aspas para garantir que o nome da variável seja interpretado corretamente

      - name: Get Last Commit Information
        id: commit_info # Adicionando um ID para referenciar os outputs
        run: |
          # Salva as informações do commit como outputs do passo
          echo "COMMIT_HASH=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"
          echo "COMMIT_AUTHOR=$(git log -1 --pretty=format:'%an')" >> "$GITHUB_OUTPUT"
          echo "COMMIT_MESSAGE=$(git log -1 --pretty=format:'%s')" >> "$GITHUB_OUTPUT"
          echo "COMMIT_DATE=$(git log -1 --pretty=format:'%ci')" >> "$GITHUB_OUTPUT"
          
      - name: Create or Update Pull Request
        # O GitHub CLI (gh) usa GH_TOKEN para autenticação,
        # mas ele também pode usar o GITHUB_TOKEN padrão ou o FORK_REPO_TOKEN que definimos.
        # Estamos explicitamente usando o FORK_REPO_TOKEN aqui.
        env:
          GH_TOKEN: ${{ secrets.FORK_REPO_TOKEN }}
          # Referencia as variáveis de ambiente e outputs dos passos anteriores
          BRANCH_NAME: ${{ env.BRANCH_NAME }}
          COMMIT_HASH: ${{ steps.commit_info.outputs.COMMIT_HASH }}
          COMMIT_AUTHOR: ${{ steps.commit_info.outputs.COMMIT_AUTHOR }}
          COMMIT_MESSAGE: ${{ steps.commit_info.outputs.COMMIT_MESSAGE }}
          COMMIT_DATE: ${{ steps.commit_info.outputs.COMMIT_DATE }}
        run: |
          echo "Creating PR from $BRANCH_NAME to 'release' in fork repository..."
          
          # Monta o corpo do PR
          PR_BODY="## Sincronização automática do repositório privado

          **Commit:** \`$COMMIT_HASH\`
          **Autor:** $COMMIT_AUTHOR
          **Data:** $COMMIT_DATE
          **Mensagem:** $COMMIT_MESSAGE
          
          Este PR foi criado automaticamente após um push na branch \`release\` do repositório privado.
          
          **Repositório origem:** bonanza-ong/olimpia-manager-na"

          # Tenta criar o PR. Se falhar, é porque provavelmente já existe.
          # O '--repo' especifica o repositório onde o PR será criado.
          gh pr create \
            --repo "EECC-NA-POA/olimpia-manager-eecc" \
            --base "release" \
            --head "$BRANCH_NAME" \
            --title "🔄 Sincronização: $COMMIT_MESSAGE" \
            --body "$PR_BODY" || echo "PR já existe ou ocorreu um erro. O workflow continuará."

      - name: Final Success Notification
        if: success()
        run: |
          echo "✅ Workflow completed successfully!"
          echo "Uma branch foi enviada e um Pull Request foi criado/atualizado no repositório EECC-NA-POA/olimpia-manager-eecc."
